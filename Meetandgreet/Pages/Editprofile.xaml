<Page x:Class="Meetandgreet.Pages.Editprofile"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Meetandgreet.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Editprofile">

    <Grid>
        <StackPanel Margin="40" MaxWidth="500">
            <Grid HorizontalAlignment="Center" Margin="0,0,0,20">
                <Ellipse Width="110" Height="110" Stroke="#F7E7CE" StrokeThickness="2" StrokeDashArray="4 2">
                    <Ellipse.Fill>
                        <ImageBrush x:Name="ProfileImageBrush" ImageSource="/Images/defaultprofile.png" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <Button Content="+" Width="30" Height="30" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                Background="#FF4D4D" Foreground="White" BorderThickness="0" Click="UploadPic_Click" Cursor="Hand">
                    <Button.Resources>
                    </Button.Resources>
                </Button>
            </Grid>

            <TextBlock Text="Edit Bio:" Foreground="White"/>
            <TextBox x:Name="BioInput" Height="100" AcceptsReturn="True" TextWrapping="Wrap"/>

            <Button Content="Save Changes" Click="SaveBtn_Click" Background="#FF4D4D" Foreground="White" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>
